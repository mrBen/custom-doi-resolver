{
  "devDependencies": {
    "web-ext": "^6.7.0"
  },
  "scripts": {
    "start:firefox": "web-ext run",
    "lint": "web-ext lint",
    "build": "web-ext build --ignore-files LICENSES package*.json README.md"
  }
}
